// Placeholder only: remote avatar image branch skipped due to jsdom limitations.
describe("Navbar avatar branches placeholder", () => {
  test("placeholder passes", () => {
    expect(true).toBe(true);
  });
});
